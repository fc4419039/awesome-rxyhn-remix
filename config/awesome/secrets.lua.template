-- Claves personales — renombra este archivo a secrets.lua
-- (está en .gitignore, no se sube al repo)

return {
    openweathermap_key = "tu_api_key_de_openweathermap",
    openweathermap_city_id = "tu_city_id",
    weather_units = "metric",
}
