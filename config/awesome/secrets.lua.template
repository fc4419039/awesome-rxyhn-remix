-- Claves personales — renombra este archivo a secrets.lua
-- (está en .gitignore, no se sube al repo)

return {
    -- El clima usa Open-Meteo (sin API key) + ubicación automática por IP.
    weather_units = "metric", -- "metric" (°C) o "imperial" (°F)
    -- Déjala en nil para usar la ubicación detectada por la red,
    -- o pon una ciudad para forzarla (p. ej. "tu_city").
    weather_location_override = nil,
}
