# Arch Linux / Manjaro / EndeavourOS / Garuda / Artix
# Instalar con: paru -S --needed $(grep -v '^#' docs/deps-arch.txt | sed 's/#.*//' | grep -v '^AUR:')
# O manualmente: copiar paquetes AUR a un directorio y makepkg -si cada uno

# ==================== CORE WM ====================
awesome-git                    # AwesomeWM git version (recomendado) - AUR
picom-git                      # Compositor con blur/transparencia - AUR
kitty                          # Terminal GPU-accelerated
rofi                           # Launcher/menu
# Nerd Fonts (requieren AUR helper o instalación manual):
ttf-jetbrains-mono-nerd        # Fuente principal (iconos + texto) - AUR
ttf-iosevka-nerd               # Fuente alternativa - AUR
ttf-hack-nerd                  # Fuente fallback - AUR
ttf-font-awesome               # Iconos FontAwesome
ttf-material-design-icons      # Material Icons
ttf-weather-icons              # Weather icons - AUR

# ==================== SYSTEM / UTILS ====================
acpi                           # Battery info
acpid                          # ACPI daemon
wireless_tools                 # iwconfig, etc.
jq                             # JSON processor
inotify-tools                  # inotifywait
polkit-gnome                   # Polkit agent GUI
xdotool                        # X11 automation
xclip                          # Clipboard
maim                           # Screenshots
cliphist                       # Clipboard history (Wayland/X11)
brightnessctl                  # Brightness control
alsa-utils                     # amixer, aplay
alsa-tools                     # hdajackretask, etc.
pipewire                       # Audio server
pipewire-pulse                 # PulseAudio compat
wireplumber                    # Session manager
libpulse                       # PulseAudio libs
qt5-imageformats               # Qt5 image plugins
qt6-imageformats               # Qt6 image plugins
playerctl                      # MPRIS control
curl                           # HTTP client
ffmpeg                         # Video/audio processing
gpick                          # Color picker
imagemagick                    # Image processing (convert)
thunar                         # File manager
firefox                        # Web browser
xorg-server                    # X11 server
xorg-xrdb                      # X resources
xorg-xauth                     # X auth
xorg-xrandr                    # RandR
xorg-setxkbmap                 # Keymap
xorg-xset                      # X settings
xorg-xprop                     # X properties
xdg-utils                      # xdg-open, etc.
xdg-user-dirs                  # User dirs
bc                             # Calculator
pacman-contrib                 # paccache, pactree
upower                         # Power management
autorandr                      # Monitor profiles
udiskie                        # Auto-mount
udisks2                        # Disk management
git                            # Git
rsync                          # Sync tool

# ==================== APPS ====================
spotify                        # Spotify client - AUR
# onlyoffice-bin REMOVIDO (no necesario)
mpd                            # Music daemon
mpc                            # MPD client
ncmpcpp                        # MPD TUI client
mpd-mpris                      # MPD -> MPRIS bridge - AUR
blueman                        # Bluetooth GUI
pasystray                      # PulseAudio tray
touchegg                       # Touchpad gestures - AUR
redshift                       # Color temperature
networkmanager                 # Network management
bluez                          # Bluetooth stack
bluez-utils                    # bluetoothctl
libnotify                      # notify-send
sound-theme-freedesktop        # System sounds

# ==================== SHELL / EDITOR ====================
zsh                            # Shell
zsh-syntax-highlighting        # Fish-like highlighting
zsh-autosuggestions            # Autosuggestions
zsh-sudo                       # sudo prefix
zoxide                         # Smart cd
fzf                            # Fuzzy finder
feh                            # Wallpaper setter
neovim                         # Editor
btop                           # System monitor
lsd                            # ls replacement
bat                            # cat replacement

# ==================== LUA / DEPS ====================
python-gobject                 # GObject introspection
python-dbus                    # DBus bindings
pipewire-alsa                  # ALSA plugin
lua                            # Lua 5.4
luarocks                       # Lua package manager
pam                            # PAM headers (para lua-pam)

# ==================== LOGIN / LOCK ====================
i3lock                         # Screen locker
slock                          # Simple locker
xsecurelock                    # Secure locker - AUR
sddm                           # Display manager
qt6-declarative                # Qt6 QML (SDDM theme)

# ==================== AUR ONLY (paru/yay) ====================
# Los siguientes paquetes SOLO están en AUR.
# Si no tienes AUR helper, instálalos manualmente:
#   git clone https://aur.archlinux.org/<paquete>.git && cd <paquete> && makepkg -si
# awesome-git                    # Git version (recomendado) - AUR
# picom-git                      # Git version con fixes blur - AUR
# ttf-jetbrains-mono-nerd        # Nerd Font completa - AUR
# ttf-iosevka-nerd               # Nerd Font alternativa - AUR
# ttf-hack-nerd                  # Nerd Font fallback - AUR
# ttf-weather-icons              # Weather icons - AUR
# mpd-mpris                      # MPD -> MPRIS bridge - AUR
# touchegg                       # Gestos touchpad - AUR
# xsecurelock                    # Secure locker - AUR
# zsh-theme-powerlevel10k        # Tema zsh - git clone https://github.com/romkatv/powerlevel10k.git ${ZSH_CUSTOM:-$HOME/.oh-my-zsh/custom}/themes/powerlevel10k
# todo-bin                       # CLI todo - cargo install todo-bin (requiere rust)